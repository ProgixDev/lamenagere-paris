# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android
/NOQTA LEGAL
# heavy product videos — manage via CDN/admin instead of git
*.mp4
.env
.env
.env
daily-report-2026-06-27.md
daily-report-2026-07-01.md
/web
# Google Play submission credential
play-service-account.json
# Build artefacts. Named individually before, which let a 127 MB APK reach a
# commit and block the push — GitHub refuses any commit holding a file over
# 100 MB even once a later commit deletes it.
*.apk
*.aab
daily-report-2026-07-30.md

# 3D bench (generated by npm run bench)
bench/
